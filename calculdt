def calculdt(vx,vy,C_FL)
    vmax_x = np.maximum(vx)
    vmax_y = np.maximum(vy)
    if vmax_x > vmax_y:
        dt = C_FL*dx/vmax_x
    if vmax_y > vmax_y:
        dt = C_FL*dx/vmax_y
    if vmax_x = vmax_y:
        dt = C_FL*dx/vmax_x
    
    return dt
    
